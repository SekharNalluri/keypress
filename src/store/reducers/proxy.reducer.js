//import {} from '../actions/proxy.actions';

const initialState = {
  loading: false,
  proxy: {},
  error: null,
};

export default (state = initialState, action) => {
  switch (action.type) {
  }
};
